AQS 即 Abstract Queued Synchronizer 是一个抽象的队列同步器，它是一个抽象类
AQS 的执行流程，建议从网上找着看，它的流程图，他也有尝试自旋，但不是一直自旋，而是尝试几次后就被判定为无法获得锁，进行阻塞，等待唤醒。

流程：
    首先一个线程去获取锁，获取失败就自旋，自旋几次后还是失败就进双向链表队列，成为节点
如果上一个节点是头节点，再次去尝试拿到锁，根据头节点状态的不同决定是否拿锁失败后继续拿锁。

AQS 难点在于看源码